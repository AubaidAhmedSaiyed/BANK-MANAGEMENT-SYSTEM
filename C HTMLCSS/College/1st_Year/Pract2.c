#include <stdio.h>

int main(){
    // This program is prepared by Aubaid Ahmed
    // length of 50 meters and the breadth of 30 meters-a program to calculate the area and the perimeter 

    int length = 50;  // decalring variables
    int breadth = 30;
    int perimeter,area;

    perimeter = 2*(length + breadth); // calculating perimeter
    area = length*breadth; // calculating area

    printf("Perimeter is : %d\n",perimeter); // printing perimeter
    printf("Area is : %d\n",area);  // printing area

    printf("24DIT063_Aubaid Ahmed");
    return 0;
}
